# 03 — SaaS Benchmark

**Status:** Not started

**Data:** SEC XBRL Frames API — no download needed, pulled live via API (free, keyless).

**Target finding:** Benchmark SaaS company financial metrics (e.g. revenue growth, gross margin) across public filers.

**Stages:**
1. Pull XBRL frames via API
2. Parse & normalize
3. Compute benchmark metrics
4. Visualize
5. Write up findings